name: "Publish release candidate"
description: "Compute next version → create & push RC git tag"

runs:
  using: composite
  steps:
    - uses: ./.github/actions/version
      id: version

    - uses: ./.github/actions/create-tag
      with:
        tag: ${{ steps.version.outputs.rc_tag }}
