name: "Integration test"
description: "Run integration tests against RC Docker images (TODO)"

runs:
  using: composite
  steps:
    - shell: bash
      run: echo "::error::No integration tests implemented yet. Add tests to .github/actions/integration-test/action.yml."
        exit 1
