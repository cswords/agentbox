name: "Integration test"
description: "Run integration tests against RC Docker images (TODO)"

runs:
  using: composite
  steps:
    - shell: bash
      run: echo "::warning::Integration tests — TODO placeholder; auto-pass for now"
